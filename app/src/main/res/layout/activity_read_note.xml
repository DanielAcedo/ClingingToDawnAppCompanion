<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_read_note"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    android:background="@color/tab_content_background_dark"
    tools:context="com.danielacedo.clingingtodawn.ReadNoteActivity">

    <TextView
        android:text="Diario del mayordomo"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:gravity="center"
        android:textSize="18sp"
        android:textColor="@color/inventoryObjectTitle"
        android:background="#563030"
        android:id="@+id/txv_readNote_title" />
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:fillViewport="true"
        android:id="@+id/scv_content">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="7dp"
            android:textColor="@color/readNote_content_textColor"
            android:background="@drawable/readnote_content_element"
            android:id="@+id/txv_readNote_content"/>
    </ScrollView>
</LinearLayout>
