<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/tabBackground_selected_dark</item>
        <item name="colorPrimaryDark">@color/tab_content_background_dark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionBarStyle">@style/ActionBarTheme</item>
        <item name="actionBarStyle">@style/ActionBarTheme</item>
    </style>

    <style name="ActionBarTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:displayOptions">showHome|useLogo</item>
        <item name="displayOptions">showHome|useLogo</item>
        <item name="background">@color/tabBackground_selected_dark</item>
        <item name="android:background">@color/tabBackground_selected_dark</item>
    </style>

    <style name="AppThemeLight" parent="AppTheme" />

</resources>
